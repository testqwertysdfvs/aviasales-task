import * as React from 'react';
import s from './style.scss';

const Options = () => {
    return (
        <div className={s.Options}>
            Options
        </div>
    )
};

export default Options;
